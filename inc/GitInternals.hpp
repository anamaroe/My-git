#ifndef GIT_INTERNALS_HPP
#define GIT_INTERNALS_HPP

#include <string>
#include "../inc/Util.hpp"
#include <iostream>
#include <filesystem>
#include <fstream>
#include <string>
#include <zlib.h>

using namespace std;

class GitInternals {
public:
	// init, catfile, hashobject, lstree, writetree
};

#endif